# Webcam-Flask Project

## Description

This project is about streaming webcam through LAN using Flask

## Requirements

The project requires the following packages:

`opencv`

`Flask`
 
## Installation

Simply run `app.py` to initiate the web server.